#include<stdio.h>
#include<string.h>
int main()
{
    char a[]="kirankumar",ch='a';
    int l=strlen(a),c=0,i=0;
    for(i=0;i<l;i++)
    {
        if(a[i]==ch)
        {
            c++;
           break;
        }
        
    }
(c==0)?printf("not found"):printf("%c is at pos %d",ch,i+1);
}
    
