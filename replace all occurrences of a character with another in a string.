#include<stdio.h>
 #include<string.h>
 int main()
 {
     char s[]="kirankumar",ch='k',ch1='h';
     int l=strlen(s),c=0,i=0,j=0,n=0;
     for(i=0;i<l;i++)
     {
         if(s[i]==ch)
         {
             s[i]=ch1;
         }
        
 }
 printf("%s",s);
 }
